---
import ProgressBar from '@components/react/progressBar'

interface Props {
  progress: number,
  ceiling: number,
  header: string,
  extra: number
}

const { progress, ceiling, header, extra } = Astro.props;
---

<ProgressBar progress={progress} ceiling={ceiling} header={header} extra={extra} client:load />
